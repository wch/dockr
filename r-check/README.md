Docker image for running package checks and reverse dependency checks
=====================================================================

This is a Dockerfile for running package checks on the release version of R, and R-devel. It is based on the rocker/r-devel image.
